import React, { Component, PropTypes } from 'react'

export default class Footer extends Component {
  render() {
    const { } = this.props

    return (
      <footer>
        Footer component
      </footer>
    )
  }
}

Footer.propTypes = {}
