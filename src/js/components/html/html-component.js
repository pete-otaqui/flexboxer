import React, { Component, PropTypes } from 'react'

export default class Html extends Component {
  render() {
    const { } = this.props

    return (
      <div>
        Html component
      </div>
    )
  }
}

Html.propTypes = {}
