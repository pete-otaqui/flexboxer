import React, { Component, PropTypes } from 'react'

export default class Demo extends Component {
  render() {
    const { } = this.props

    return (
      <div className="demo">
        Demo component
      </div>
    )
  }
}

Demo.propTypes = {}