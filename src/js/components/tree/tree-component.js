import React, { Component, PropTypes } from 'react'

export default class Tree extends Component {
  render() {
    const { } = this.props

    return (
      <div className="tree">
        Tree component
      </div>
    )
  }
}

Tree.propTypes = {}
