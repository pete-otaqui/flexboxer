import React, { Component, PropTypes } from 'react'

export default class Css extends Component {
  render() {
    const { } = this.props

    return (
      <div>
        Css component
      </div>
    )
  }
}

Css.propTypes = {}
